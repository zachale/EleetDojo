import 'package:flutter/material.dart';

class SenseiPage extends StatelessWidget {
  const SenseiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Sensei Page - Coming Soon'),
      ),
    );
  }
}
